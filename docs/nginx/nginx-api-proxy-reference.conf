# 🚀 ZINEINSIGHT API PROXY CONFIGURATION
# =====================================
# Ajouter cette configuration à votre nginx principal pour zineinsight.com

# Proxy API vers backend Revolutionary
location /api/ {
    proxy_pass http://localhost:8000/api/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;

    # Timeout settings pour les calculs longs
    proxy_connect_timeout 30s;
    proxy_send_timeout 30s;
    proxy_read_timeout 30s;

    # Headers CORS si nécessaire
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
    add_header Access-Control-Allow-Headers 'Content-Type, Authorization';
}

# Questionnaire et pages statiques
location / {
    root /var/www/Revolutionnary/platform/frontend/spa;
    try_files $uri $uri/ /index.html;

    # Cache statique
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico)$ {
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
}
